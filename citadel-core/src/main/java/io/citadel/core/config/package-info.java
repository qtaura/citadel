/**
 * Configuration loading, parsing, validation, and access.
 *
 * <p>This package contains the implementation of the {@link io.citadel.api.service.Configuration}
 * service. It provides YAML-based file loading, typed accessors with descriptive error messages,
 * default configuration generation, and programmatic reload with listener notification.
 *
 * <p>The package is internal to {@code citadel-core} and must not be referenced by plugins.
 */
package io.citadel.core.config;
